name: Publish to GitHub Packages

on:
    push:
        tags:
            - '*'
    workflow_dispatch:
        inputs:
            dist_tag:
                description: 'npm dist-tag (e.g. beta, beta1). Leave blank to publish as latest.'
                required: false
                default: ''
                type: string

permissions:
    contents: read
    packages: write

jobs:
    publish:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - uses: pnpm/action-setup@v4

            - uses: actions/setup-node@v4
              with:
                  node-version-file: .nvmrc
                  cache: pnpm
                  registry-url: 'https://npm.pkg.github.com'
                  scope: 'troychaplin'

            - run: pnpm install --frozen-lockfile
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - run: pnpm build

            - run: pnpm exec playwright install chromium --with-deps
            - run: pnpm test --run

            - name: Publish package
              run: |
                  DIST_TAG="${{ github.event.inputs.dist_tag }}"
                  if [ -n "$DIST_TAG" ]; then
                    pnpm publish --access restricted --no-git-checks --tag "$DIST_TAG"
                  else
                    pnpm publish --access restricted --no-git-checks
                  fi
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
