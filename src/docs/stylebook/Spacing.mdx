import { Meta } from '@storybook/addon-docs/blocks';
import { SpacingScale, PaddingScale } from './components/SpacingScale';

<Meta title="Overview/Stylebook/Spacing" />

# Spacing

Seven spacing levels from `2-x-small` to `2-x-large`. Several use viewport-relative
`min()` values for fluid scaling.

<SpacingScale
    title="Gap Scale"
    description="Two blocks separated by a gap equal to the token value — how spacing reads when used as flex/grid gap."
/>

<PaddingScale
    title="Padding Scale"
    description="A content block wrapped in padding equal to the token value — how spacing reads when used inside a container."
/>
