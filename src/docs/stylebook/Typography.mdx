import { Meta } from '@storybook/addon-docs/blocks';
import { FontFamilies, BaseStyles, FontSizeTokens } from './components/TypographyScale';

<Meta title="Overview/Stylebook/Typography" />

# Typography

Typography from `c2b.config.json` — the font families available, the element
styles applied by `baseStyles`, and the underlying responsive size scale.

<FontFamilies
    title="Font Families"
    description="Available font family tokens with a sample paragraph in each."
/>

<BaseStyles
    title="Base Styles"
    description="Typography applied to body and h1–h6 by baseStyles, showing the font-size token behind each element."
/>

<FontSizeTokens
    title="Font Size Tokens"
    description="The full size scale. Each token is a clamp() value that scales between a min and a max."
    filter="body"
/>
